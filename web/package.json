{
  "name": "visionclaw-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phone-first PWA for the VisionClaw AI employee. Talks only to the existing gateway.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "npm run build && vitest run",
    "e2e": "node e2e/run.mjs"
  },
  "dependencies": {
    "livekit-client": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "jsdom": "^30.0.1",
    "typescript": "^5.9.3",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
